# Empty uses the same-origin /api proxy (recommended for Docker/Nginx deployments).
# For a separately deployed API, set this to its public HTTPS URL without a trailing slash.
VITE_API_BASE_URL=
# Used only by `npm run dev`; production uses VITE_API_BASE_URL.
VITE_DEV_API_URL=http://localhost:4000
